#alias ls="command ls -AF --group-directories-first --color=auto"
#alias ll="command ls -lhAF --group-directories-first --color=auto"
alias cdup="cd .."

#alias cp="command cp -i"
#alias mv="command mv -i"
#alias mkdir="command mkdir -pv"

alias vim="nvim"

alias venv="source venv/bin/activate.fish"
alias tmux-kill="tmux kill-session"

alias shellcaster="shellcaster -c /automnt/extMedia/.config/shellcaster/config.toml"

alias pm="pacman"

alias xc="xclip -selection clipboard"

alias gitdiff="GIT_EXTERNAL_DIFF=difft git diff"

# for editing mods for Elder Scrolls: Oblivion
alias wrye="STEAM_COMPAT_DATA_PATH=~/.local/share/Steam/steamapps/compatdata/22330/ ~/.local/share/Steam/steamapps/common/Proton\ 5.13/proton run ~/.local/share/Steam/steamapps/common/Oblivion/Mopy/Wrye\ Bash.exe"
